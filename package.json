{
  "name": "server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "dev": "bun run index.ts",
    "build": "bun build --outfile=dist/main.js index.ts",
    "start": "bun run build && bun run dist/main.js",
    "watch": "bun --watch index.ts"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@google-cloud/vertexai": "^1.10.0",
    "ioredis": "^5.8.2",
    "livekit-server-sdk": "^2.15.0"
  }
}
